node_modules/
lib/
*.log
.DS_Store

# Probe/smoke artifacts now live in .probe/ (scripts/paths.mjs). The old
# root-level names are kept so an existing checkout stays clean.
.probe/
probe-orca-app*.log
probe-last-screen.txt
probe-last-raw.log
probe-scrollback-last.txt
live-dump.txt
e2e-last-buffer.log
probe-attach-sample.txt
.probe-crops/
